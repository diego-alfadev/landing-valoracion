---
const steps = [
  {
    number: 1,
    title: "Introduce la dirección del inmueble",
    description: "Utilizaremos esta información para realizar encontrar tu inmueble en la base de datos del catastro",
  },
  {
    number: 2,
    title: "Responde a un sencillo cuestionario",
    description: "Describe las particularidades de tu inmueble.",
  },
  {
    number: 3,
    title: "Indica el correo electrónico al que quieres que te lo enviemos",
    description: "No te preocupes, no te enviaremos spam. Solo la valoración de tu inmueble.",
  },
  {
    number: 4,
    title: "¡Listo!",
    description: "La valoración de tu inmueble estará en tu bandeja de entrada",
    step_data: {
      isCompleted: true,
    },
  },
];
---

<section class="w-full flex flex-col justify-center items-center py-10">
  <h1 class="section-title">
    Obtén tu valoración en {steps.length} simples pasos
  </h1>

  <!-- Stepper -->
  <ul class="relative flex flex-col sm:flex-row gap-8 mx-10">
    {
      steps.map((step) => (
        <li class="md:shrink md:basis-0 flex-1 group flex gap-x-10 md:block">
          <div class="min-w-7 min-h-7 flex flex-col items-center w-full md:inline-flex md:flex-wrap md:flex-row text-xs align-middle">
            {step.step_data?.isCompleted ? (
              <span class="size-16 text-4xl text-white flex justify-center font-bold items-center shrink-0 bg-green-600  rounded-full">
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  class="icon icon-tabler icon-tabler-check"
                  width="24"
                  height="24"
                  viewBox="0 0 24 24"
                  stroke-width="4"
                  stroke="currentColor"
                  fill="none"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                  <path d="M5 12l5 5l10 -10" />
                </svg>
              </span>
            ) : (
              <span class=" size-16 text-4xl text-white flex justify-center font-bold items-center shrink-0 bg-primary rounded-full">
                {step.number}
              </span>
            )}
            {/* <div class="w-full md:h-px bg-gray-00 "></div> */}
            <div class="mt-2 w-px h-full md:mt-0 md:ms-2 md:w-full md:h-px md:flex-1 bg-gray-200 group-last:hidden dark:bg-neutral-700 8" />
          </div>
          <div class="grow md:grow-0 md:mt-3 pb-5 w-full ">
            <span class="block text-sm font-medium text-gray-800">
              {step.title}
            </span>
            <p class="text-sm text-gray-500 dark:text-neutral-500">
              {step.description}
            </p>
          </div>
        </li>
      ))
    }
  </ul>
  <!-- End Stepper -->
</section>
